<div class="">
  <div class="mb-4 text-center">
    <p class="text-slate-900 dark:text-teal-200 text-xl sm:text-2xl font-medium  break-words flex items-center justify-center gap-2">
      <%= tutorial.name %>
    </p>
  </div>
  <!-- Student and Teachers Info - Stack on mobile, side by side on larger screens -->
  <div class="flex items-center justify-center gap-2 text-slate-700 dark:text-slate-300 text-sm sm:text-base lg:text-lg">
    <div class="flex items-center gap-2">
      <% if tutorial.teacher.display_avatar(size: 32) %>
        <%= image_tag tutorial.teacher.display_avatar(size: 32), class: "w-8 h-8 rounded-full object-cover border border-gray-200 dark:border-gray-700" %>
      <% end %>
      <%= link_to tutorial.teacher.name, professor_path(tutorial.teacher), class:'text-indigo-600 hover:text-indigo-500 dark:text-indigo-400 dark:hover:text-indigo-300' %>
    </div>
  </div>
</div>