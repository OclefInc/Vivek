<!DOCTYPE html>
<html>
  <head>
    <%= render "shared/google_analytics" %>
    <title><%= content_for(:title) || "The Vivek Project" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <meta name="turbo-refresh-method" content="morph">
    <meta name="turbo-refresh-scroll" content="preserve">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= yield :head %>
    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>
    <%= favicon_link_tag "favi.png" %>
    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "actiontext", "data-turbo-track": "reload" %>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/tom-select@2.3.1/dist/css/tom-select.default.min.css">
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <script src="https://cdn.jsdelivr.net/npm/pdfjs-dist@3.11.174/build/pdf.min.js"></script>
    <script defer src="https://cdn.jsdelivr.net/npm/alpinejs@3.x.x/dist/cdn.min.js"></script>
    <%= javascript_importmap_tags 'application' %>
  </head>
  <body class="flex h-screen overflow-hidden bg-white dark:bg-gray-900 admin-layout" data-controller="sidebar">
    <!-- Sidebar -->
    <aside class="bg-sky-100 dark:bg-gray-800 w-64 flex-shrink-0 hidden md:flex flex-col">
      <div class="p-5 border-b border-sky-200 dark:border-gray-700">
        <span class="text-2xl font-medium text-black dark:text-gray-100"><%= link_to("Admin", admin_path) %></span>
      </div>
      <nav class="flex-1 overflow-y-auto py-4">
        <%= link_to "Projects", assignments_path, class: nav_link_class(["/admin/assignments", "/admin/lessons", "/admin", "/admin/"]) %>
        <%= link_to "Project Types", project_types_path, class: nav_link_class(["/admin/project_types"]) %>
        <%= link_to "Students", students_path, class: nav_link_class(["/admin/students"]) %>
        <%= link_to "Teachers", teachers_path, class: nav_link_class(["/admin/teachers", "/admin/tutorials"]) %>
        <%= link_to "Skills", skill_categories_path, class: nav_link_class(["/admin/skill_categories"]) %>
        <%= link_to "Music", compositions_path, class: nav_link_class(["/admin/compositions"]) %>
        <%= link_to "Comments", admin_comments_path, class: nav_link_class(["/admin/comments"]) %>
        <%= link_to "Accounts", accounts_path, class: nav_link_class(["/admin/accounts"]) %>
        <% if Rails.env.development? %>
          <a href="/coverage/index.html" target="_blank" class="<%= nav_link_class([]) %>">Coverage</a>
        <% end %>
      </nav>
      <div class="p-5 border-t border-sky-200 dark:border-gray-700">
        <% if current_user.teacher.present? %>
          <%= link_to "Teacher Profile", teacher_path(current_user.teacher), class: "block px-5 py-2 text-cyan-600 dark:text-cyan-400 hover:text-cyan-500 dark:hover:text-cyan-300 mb-2 text-center" %>
        <% end %>
        <a href="/users/edit" class="flex items-center gap-3 px-5 py-2 text-cyan-600 dark:text-cyan-400 hover:text-cyan-500 dark:hover:text-cyan-300 mb-2">
          <div class="w-8 h-8 rounded-full overflow-hidden flex-shrink-0">
            <% if current_user.avatar.attached? %>
              <%= image_tag current_user.cropped_avatar(size: 32), class: "w-full h-full object-cover" %>
            <% elsif current_user.picture_url.present? %>
              <%= image_tag current_user.picture_url, class: "w-full h-full object-cover" %>
            <% else %>
              <div class="w-full h-full bg-gradient-to-br from-blue-400 to-purple-500 flex items-center justify-center">
                <span class="text-white text-xs font-semibold"><%= current_user.initials %></span>
              </div>
            <% end %>
          </div>
          <span>My Account</span>
        </a>
        <%= link_to "Leave Admin", root_path, class: "block px-5 py-2 text-cyan-600 dark:text-cyan-400 hover:text-cyan-500 dark:hover:text-cyan-300 mb-2 text-center" %>
        <%= button_to "Log out", destroy_user_session_path, method: :delete, class:"w-full bg-white dark:bg-gray-700 border-sky-50 dark:border-gray-600 border-2 py-2 px-3 hover:border-sky-500 dark:hover:border-gray-500 rounded-lg text-black dark:text-gray-200" %>
      </div>
    </aside>
    <!-- Mobile header bar -->
    <div class="md:hidden fixed top-0 left-0 w-full bg-sky-100 dark:bg-gray-800 border-b border-sky-200 dark:border-gray-700 z-40 p-4">
      <div class="relative flex items-center justify-center">
        <button data-action="click->sidebar#toggle" class="absolute left-0 p-2 rounded-lg hover:bg-sky-200 dark:hover:bg-gray-700">
          <svg class="w-6 h-6 dark:text-gray-200" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"></path>
          </svg>
        </button>
        <span class="text-xl font-medium text-black dark:text-gray-100">
          <%= content_for?(:page_title) ? content_for(:page_title) : (content_for?(:title) ? content_for(:title) : link_to("Admin", admin_path, class: "text-black dark:text-gray-100")) %>
        </span>
      </div>
    </div>
    <!-- Mobile sidebar overlay -->
    <div data-sidebar-target="overlay"
         class="hidden fixed inset-0 bg-black bg-opacity-50 z-40 md:hidden"
         data-action="click->sidebar#close"></div>
    <!-- Mobile sidebar -->
    <aside data-sidebar-target="menu"
           class="fixed top-0 left-0 h-full bg-sky-100 dark:bg-gray-800 w-64 z-50 md:hidden flex flex-col"
           style="transform: translateX(-100%); transition: transform 0.3s ease-in-out;">
      <div class="p-5 border-b border-sky-200 dark:border-gray-700">
        <span class="text-2xl font-medium text-black dark:text-gray-100"><%= link_to("Admin", admin_path) %></span>
      </div>
      <nav class="flex-1 overflow-y-auto py-4">
        <%= link_to "Projects", assignments_path, class: nav_link_class(["/admin/assignments", "/admin/lessons", "/admin", "/admin/"]) %>
        <%= link_to "Project Types", project_types_path, class: nav_link_class(["/admin/project_types"]) %>
        <%= link_to "Students", students_path, class: nav_link_class(["/admin/students"]) %>
        <%= link_to "Teachers", teachers_path, class: nav_link_class(["/admin/teachers", "/admin/tutorials"]) %>
        <%= link_to "Skills", skill_categories_path, class: nav_link_class(["/admin/skill_categories"]) %>
        <%= link_to "Music", compositions_path, class: nav_link_class(["/admin/compositions"]) %>
        <%= link_to "Comments", admin_comments_path, class: nav_link_class(["/comments"]) %>
        <%= link_to "Accounts", accounts_path, class: nav_link_class(["/admin/accounts"]) %>
        <% if Rails.env.development? %>
          <a href="/coverage/index.html" target="_blank" class="<%= nav_link_class([]) %>">Coverage</a>
        <% end %>
      </nav>
      <div class="p-5 border-t border-sky-200 dark:border-gray-700">
        <% if current_user.teacher.present? %>
          <%= link_to "Teacher Profile", teacher_path(current_user.teacher), class: "block px-5 py-2 text-cyan-600 dark:text-cyan-400 hover:text-cyan-500 dark:hover:text-cyan-300 mb-2 text-center" %>
        <% end %>
        <a href="/users/edit" class="flex items-center gap-3 px-5 py-2 text-cyan-600 dark:text-cyan-400 hover:text-cyan-500 dark:hover:text-cyan-300 mb-2">
          <div class="w-8 h-8 rounded-full overflow-hidden flex-shrink-0">
            <% if current_user.avatar.attached? %>
              <%= image_tag current_user.cropped_avatar(size: 32), class: "w-full h-full object-cover" %>
            <% elsif current_user.picture_url.present? %>
              <%= image_tag current_user.picture_url, class: "w-full h-full object-cover" %>
            <% else %>
              <div class="w-full h-full bg-gradient-to-br from-blue-400 to-purple-500 flex items-center justify-center">
                <span class="text-white text-xs font-semibold"><%= current_user.initials %></span>
              </div>
            <% end %>
          </div>
          <span>My Account</span>
        </a>
        <%= link_to "Leave Admin", root_path, class: "block px-5 py-2 text-cyan-600 dark:text-cyan-400 hover:text-cyan-500 dark:hover:text-cyan-300 mb-2 text-center" %>
        <%= button_to "Log out", destroy_user_session_path, method: :delete, class:"w-full bg-white dark:bg-gray-700 border-sky-50 dark:border-gray-600 border-2 py-2 px-3 hover:border-sky-500 dark:hover:border-gray-500 rounded-lg text-black dark:text-gray-200" %>
      </div>
    </aside>
    <!-- Main content area -->
    <div class="flex-1 flex flex-col overflow-hidden">
      <main class="flex-1 overflow-y-auto px-5 pb-10 pt-18 md:pt-10 md:px-10 bg-white dark:bg-gray-900">
        <p style="color: green"><%= notice %></p>
        <%= yield %>
      </main>
    </div>
  </body>
</html>
