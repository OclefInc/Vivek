<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Vivek" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= yield :head %>
    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">
    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "actiontext", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/tom-select@2.3.1/dist/css/tom-select.default.min.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/plyr@3.7.8/dist/plyr.css">
    <script src="https://cdn.jsdelivr.net/npm/pdfjs-dist@3.11.174/build/pdf.min.js"></script>
    <%= javascript_importmap_tags 'application' %>
  </head>
  <body class="flex h-screen overflow-hidden bg-white dark:bg-gray-900 admin-layout" data-controller="sidebar">
    <!-- Sidebar -->
    <aside class="bg-sky-100 dark:bg-gray-800 w-64 flex-shrink-0 hidden md:flex flex-col">
      <div class="p-5 border-b border-sky-200 dark:border-gray-700">
        <span class="text-2xl font-medium text-black dark:text-gray-100"><%= link_to("The Vivek Project", root_path) %></span>
      </div>
      <nav class="flex-1 overflow-y-auto py-4">
        <%= link_to "Assignments", assignments_path, class: nav_link_class(["/assignments", "/lessons"]) %>
        <%= link_to "Students", students_path, class: nav_link_class(["/students"]) %>
        <%= link_to "Teachers", teachers_path, class: nav_link_class(["/teachers"]) %>
        <%= link_to "Compositions", compositions_path, class: nav_link_class(["/compositions"]) %>
        <%= link_to "Skills", skills_path, class: nav_link_class(["/skills"]) %>
        <%= link_to "Comments", comments_path, class: nav_link_class(["/comments"]) %>
        <%= link_to "Accounts", accounts_path, class: nav_link_class(["/accounts"]) %>
      </nav>
      <div class="p-5 border-t border-sky-200 dark:border-gray-700">
        <a href="/users/edit" class="block px-5 py-2 text-cyan-600 dark:text-cyan-400 hover:text-cyan-500 dark:hover:text-cyan-300 mb-2">My Account</a>
        <%= button_to "Log out", destroy_user_session_path, method: :delete, class:"w-full bg-white dark:bg-gray-700 border-sky-50 dark:border-gray-600 border-2 py-2 px-3 hover:border-sky-500 dark:hover:border-gray-500 rounded-lg text-black dark:text-gray-200" %>
      </div>
    </aside>
    <!-- Mobile header bar -->
    <div class="md:hidden fixed top-0 left-0 w-full bg-sky-100 dark:bg-gray-800 border-b border-sky-200 dark:border-gray-700 z-40 p-4">
      <div class="relative flex items-center justify-center">
        <button data-action="click->sidebar#toggle" class="absolute left-0 p-2 rounded-lg hover:bg-sky-200 dark:hover:bg-gray-700">
          <svg class="w-6 h-6 dark:text-gray-200" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"></path>
          </svg>
        </button>
        <span class="text-xl font-medium text-black dark:text-gray-100">
          <%= content_for?(:page_title) ? content_for(:page_title) : (content_for?(:title) ? content_for(:title) : link_to("The Vivek Project", root_path, class: "text-black dark:text-gray-100")) %>
        </span>
      </div>
    </div>
    <!-- Mobile sidebar overlay -->
    <div data-sidebar-target="overlay"
         class="hidden fixed inset-0 bg-black bg-opacity-50 z-40 md:hidden"
         data-action="click->sidebar#close"></div>
    <!-- Mobile sidebar -->
    <aside data-sidebar-target="menu"
           class="fixed top-0 left-0 h-full bg-sky-100 dark:bg-gray-800 w-64 z-50 md:hidden flex flex-col"
           style="transform: translateX(-100%); transition: transform 0.3s ease-in-out;">
      <div class="p-5 border-b border-sky-200 dark:border-gray-700">
        <span class="text-2xl font-medium text-black dark:text-gray-100">The Vivek Project</span>
      </div>
      <nav class="flex-1 overflow-y-auto py-4">
        <%= link_to "Assignments", assignments_path, class: nav_link_class(["/assignments", "/lessons"]) %>
        <%= link_to "Students", students_path, class: nav_link_class(["/students"]) %>
        <%= link_to "Teachers", teachers_path, class: nav_link_class(["/teachers"]) %>
        <%= link_to "Compositions", compositions_path, class: nav_link_class(["/compositions"]) %>
        <%= link_to "Skills", skills_path, class: nav_link_class(["/skills"]) %>
        <%= link_to "Comments", comments_path, class: nav_link_class(["/comments"]) %>
        <%= link_to "Accounts", accounts_path, class: nav_link_class(["/accounts"]) %>
      </nav>
      <div class="p-5 border-t border-sky-200 dark:border-gray-700">
        <a href="/users/edit" class="block px-5 py-2 text-cyan-600 dark:text-cyan-400 hover:text-cyan-500 dark:hover:text-cyan-300 mb-2">My Account</a>
        <%= button_to "Log out", destroy_user_session_path, method: :delete, class:"w-full bg-white dark:bg-gray-700 border-sky-50 dark:border-gray-600 border-2 py-2 px-3 hover:border-sky-500 dark:hover:border-gray-500 rounded-lg text-black dark:text-gray-200" %>
      </div>
    </aside>
    <!-- Main content area -->
    <div class="flex-1 flex flex-col overflow-hidden">
      <main class="flex-1 overflow-y-auto px-5 pb-10 pt-18 md:pt-10 md:px-10 bg-white dark:bg-gray-900">
        <p style="color: green"><%= notice %></p>
        <%= yield %>
      </main>
    </div>
  </body>
</html>
