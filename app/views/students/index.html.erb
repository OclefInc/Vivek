<%= render layout: "shared/index_layout",
           locals: {
             title: "Students",
             search_url: students_path,
             turbo_frame: "students-table",
             search_placeholder: "Search for students",
             new_button_text: "Add new student",
             new_path: new_student_path
           } do %>
  <table class="w-full table-auto border-collapse">
    <tr>
      <th class="text-left px-3 py-2 border-b border-gray-300 dark:border-gray-600 dark:text-gray-200">Name</th>
      <th class="text-left px-3 py-2 border-b border-gray-300 dark:border-gray-600 dark:text-gray-200">Projects</th>
    </tr>
    <% @students.each do |student| %>
      <tr>
        <td class="px-3 py-2 border-b border-gray-300 dark:border-gray-600 dark:text-gray-300"><%= link_to student.name, student, class:"text-blue-500 text-semibold hover:text-red-500 dark:text-blue-400 dark:hover:text-red-400 px-1 py-1" %></td>
        <td class="px-3 py-2 border-b border-gray-300 dark:border-gray-600 dark:text-gray-300"><%= student.assignments.length %></td>
      </tr>
    <% end %>
  </table>
<% end %>
