<style>
  .project-types *:not(h1):not(h2):not(h3) {
    font-family: Georgia, Times, 'Times New Roman', serif !important;
  }
  .project-types div.description {
    font-size: 1.25rem;
    line-height: 1.8;
  }
  .project-types p {
    font-size: 1.25rem;
    line-height: 1.8;
  }
</style>
<div class="max-w-4xl mx-auto px-3 project-types">
  <div class="mb-8 text-center">
    <h1 class="text-center text-3xl md:text-4xl font-bold text-sky-800 dark:text-teal-200">Project Types</h1>
  </div>
  <div class="prose prose-lg dark:prose-invert max-w-none space-y-6 text-gray-800 dark:text-gray-200 [&_p]:mb-4">
    <section>
      <h2 class="text-2xl font-semibold text-sky-800 dark:text-teal-300 mb-4">Why Different Project Types Matter</h2>
      <p>
        In the realm of piano pedagogy, understanding the diverse needs of students is crucial. Each project type has a specific role in shaping the learning experience. By categorizing projects into distinct types, educators and contributors can better appreciate the multifaceted nature of piano education and tailor their teaching approaches accordingly.
      </p>
      <p>
        This classification highlights the focus of teachers at various stages in students' piano journey.  We hope to foster a discussion of pedagogical practices within the community.
      </p>
    </section>
    <% @project_types.each do |project_type| %>
      <% cache project_type do %>
        <% if project_type.assignments.any? %>
          <section class="bg-gray-50 dark:bg-gray-800 p-6  rounded-lg border-2 border-blue-200 dark:border-indigo-700">
            <div class="flex items-center gap-3 mb-4">
              <div class="text-indigo-600 dark:text-indigo-400">
                <%= project_type_icon(project_type) %>
              </div>
              <h2 class="text-2xl font-bold text-sky-800 dark:text-teal-200 m-0"><%= project_type.name %></h2>
            </div>
            <div class="description">
              <%= project_type.description %>
            </div>
            <div class="text-center mt-6">
              <%= link_to("View #{pluralize(project_type.assignments.count, "Project")}", public_project_type_path(project_type), class: "inline-block px-6 py-2 rounded-full bg-indigo-600 text-white hover:bg-indigo-700 dark:bg-indigo-500 dark:hover:bg-indigo-600 transition-colors duration-200 font-semibold no-underline", style: "font-family: ui-sans-serif, system-ui, sans-serif !important;") %>
            </div>
          </section>
        <% end %>
      <% end %>
    <% end %>
  </div>
</div>