<p style="color: green"><%= notice %></p>
<div class="flex justify-between ">
  <h1 class="font-bold text-3xl">Assignment:</h1>
  <div class="ml-2 flex items-start justify-end gap-1">
    <%= link_to "Edit", edit_assignment_path(@assignment), class: "sm:w-auto text-center rounded-lg px-3 py-2 m-1 bg-gray-100 hover:bg-gray-50 inline-block font-medium" %>
    <%= link_to "All assignments", assignments_path, class: "sm:w-auto text-center text-nowrap rounded-lg px-3 py-2 m-1 bg-gray-100 hover:bg-gray-50 inline-block font-medium" %>
    <%= button_to "Delete", @assignment, method: :delete, class: "sm:w-auto rounded-lg px-3 py-2 m-1 text-white bg-red-600 hover:bg-red-500 font-medium cursor-pointer", data: { turbo_confirm: "Delete entire assignment; Are you sure?" } %>
  </div>
</div>
<div class="flex">
  <%= render @assignment %>
  <div>
    <div class="flex justify-between items-center my-4 sm:flex-wrap">
      <h1 class="text-2xl text-red-500">Assignment Lessons:</h1>
    </div>
    <div class="max-w-5xl mx-auto px-8 py-4">
      <table class="w-full table-auto border-collapse">
        <thead>
          <tr>
            <th class="text-left px-3 py-2 border-b border-gray-300">Name</th>
            <th class="text-left px-3 py-2 border-b border-gray-300">Status</th>
          </tr>
        </thead>
        <tbody data-controller="sortable">
          <% @assignment.lessons.order(:sort).each_with_sortable_id do |lesson, sortable_id| %>
            <tr id="<%= sortable_id %>">
              <td class="text-left px-3 py-2 border-b border-gray-300"><%= link_to lesson.name, lesson, class:"text-blue-500 hover:text-red-500"  %></td>
              <td class="text-left px-3 py-2 border-b border-gray-300"><%= lesson.status %></td>
            </tr>
          <% end %>
        </tbody>
      </table>
      <%= link_to "Add New Lesson", new_lesson_path({assignment_id:@assignment.id}), class: "rounded-lg px-3 py-2 bg-blue-600 hover:bg-blue-400 text-white font-medium" %>
    </div>
  </div>
</div>
<%= render partial:"/comments/module", locals:{annotation:@assignment} %>
