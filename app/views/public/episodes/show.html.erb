<% content_for :title, 'Episode' %>
<%= render 'header' %>
<div class="xl:flex xl:gap-8 py-4">
  <div class="xl:min-w-[640px] xl:max-w-[640px] md:flex-1 md:basis-1/2 mb-4 xl:mb-0">
    <%= render 'video_description' %>
    <!-- Comments Section -->
    <div class="w-full mt-8">
      <%= render partial:"/public/comments/module", locals:{annotation:@episode} %>
    </div>
  </div>
  <div class="w-full xl:flex-1 xl:basis-1/2 xl:self-start overflow-y-auto">
    <%= render 'episode_table' %>
  </div>
</div>